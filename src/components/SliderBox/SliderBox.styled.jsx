import styled from 'styled-components';

export const DataSliderBox = styled.div`
  padding-top:22px;
  
`
export const DataSliderTitle = styled.div`
  font-style: normal;
  font-weight: 400;
  font-size: 12px;
  line-height: 14px;
  align-items: center;
  text-align: center;

  letter-spacing: 0.04em;
  color: rgba(82, 85, 95, 0.7);

`

export const DataSlider  = styled.div`
    display: flex;
    width: 170px;
    justify-content: space-evenly;
    align-items: center;

    margin-top:5px;

`

export const Period = styled.p`
  font-style: normal;
  font-weight: 700;
  font-size: 14px;
  line-height: 16px;
  letter-spacing: 0.02em;
  text-transform: uppercase;

  color: #000000;
`